out = ''
for _ in range(input()):
    s = raw_input()
    for i in range(len(s)):
        if i % 2 == 0:
            out += s[i]
    out += ' '
    for j in range(len(s)):
        if j % 2 != 0:
            out += s[j]
    out += '\n'

print out
